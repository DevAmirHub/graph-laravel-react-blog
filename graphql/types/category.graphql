type Category {
    id: ID!
    name: String!
    slug: String!
    description: String
    postsCount: Int! @count(relation: "posts")
    posts: [Post!]! @hasMany
    created_at: DateTime!
    updated_at: DateTime!
}