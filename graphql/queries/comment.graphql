extend type Query {
    "List comments with optional filters."
    comments(
        post_id: ID @eq
        status: String @eq
    ): [Comment!]! @paginate(defaultCount: 10) @orderBy(column: "created_at", direction: DESC)

    "Find a single comment by ID."
    comment(id: ID! @eq): Comment @find
}
